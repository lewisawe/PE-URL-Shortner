global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
  receiver: discord

receivers:
  - name: discord
    webhook_configs:
      - url: 'http://discord-notifier:5001/alert'
        send_resolved: true
